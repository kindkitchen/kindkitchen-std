/**
 * Options accepted by the _place_some-feature_holder_ feature.
 * Each `_place_..._holder_` token is rewritten by plopper at generate time.
 */
export interface _place_SomeFeature_holder_Options {
    /** Human-readable label for this _place_some-feature_holder_. */
    label: string;
    /** Whether the feature is active. */
    enabled: boolean;
}
