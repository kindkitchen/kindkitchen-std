---
kind: draft
date: 2026-06-30
---

# Help / usage DevX improvements

Collected while injecting the real version into the help banner.

## Must fix first: path-is-relative-to-config, not project root

Current copy is misleading. In `cli.ts` `HELP_TEXT` the `--config` block and the
`DEFAULT_CONFIG_TEXT` comments state paths are "relative to project's root":

- `globs` / `globs_to_exclude` comment: "IMPORTANT! The path is relative to
  project's root!"
- `names` comment: "IMPORTANT! The path is relative to project's root!"

But resolution is anchored on the detected config file location, not the project
root. Reword to say paths are relative to the config file's location. Apply the
same fix to the mirrored assets `help.txt` and `default_config.toml`.

Action: verify exact behavior in `cli.ts` (config path join) and
`process_machine_file.ts` before finalizing wording.

## Other DevX ideas (triage later)

- Add a real `--version` flag (now version only appears in the help banner).
- Per-command usage / examples section (`generate`, `--init`).
- Keep README help block auto-aligned with live `--help` output to avoid drift.
- Consider a short "paths are config-relative" note near each path option.
