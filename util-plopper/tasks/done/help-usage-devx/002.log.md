---
kind: log
date: 2026-07-09
---

# Fixed path wording; added `as const` to generated `const x`

## Path wording fix

Verified actual resolution in `cli.ts`: config path is `path.join(cwd, config)`,
machine `names` are `path.join(cwd, machine_name)`, and `globs` expand with
`root: cwd`. So paths are relative to the **current working directory** (where
x_plopper is executed) — not the project root (as the old copy claimed) and not
the config file location (as [[001.draft]] assumed).

Updated all four surfaces consistently:

- `cli.ts` `HELP_TEXT` — added note under `--config` that the path resolves from
  the cwd.
- `cli.ts` `DEFAULT_CONFIG_TEXT` — reworded both `globs` and `names` comments.
- `help.txt` — mirrored the `--config` note.
- `default_config.toml` — mirrored both comment rewords.

## `as const` suffix (new user request, folded into this task)

`process_machine_file.ts` now emits `const x = { ... } as const` instead of
`const x = { ... }`. Updated `fixture/machine.ts` and assertions in
`cli.test.ts` and `process_machine_file.test.ts`. All tests pass.

## Not done (deliberately)

Draft's triage-later ideas remain open: `--version` flag, per-command
usage/examples, README/help auto-alignment. Task completed on user request; spin
a new task if these become wanted.

status: todo -> done (user requested completion; must-fix item delivered)
